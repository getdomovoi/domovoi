---
"@getdomovoi/desktop": patch
"@getdomovoi/ui": patch
---

On macOS the titlebar content starts past the window buttons by an inset derived from where the desktop placed them, so the mark no longer lands on the green button. Windows and Linux get no inset.
