---
"@getdomovoi/ui": patch
"@getdomovoi/desktop": patch
---

Settings gains About this build: the daemon's version and source commit, that this build is not signed and does not update itself, and the release page where new versions come from. The desktop opens that one fixed address in the browser; a browser tab links it.
