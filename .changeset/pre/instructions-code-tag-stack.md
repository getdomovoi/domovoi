---
"@getdomovoi/daemon": patch
---

An inline HTML code tag that opens inside emphasis and closes after it still hides the import it
encloses, and a closing tag of a different element no longer ends it.
