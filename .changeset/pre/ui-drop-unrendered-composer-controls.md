---
---

Unreachable UI code only: removes the unrendered composer skill chip, the unrendered composer archive dialog and two Thread props nothing read. Archive still runs from the sessions drawer, and its confirmation test now drives that dialog. `@getdomovoi/ui` exports none of these. No package changes.
