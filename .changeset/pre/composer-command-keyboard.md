---
"@getdomovoi/ui": minor
---

Make the composer command list usable from the keyboard. Arrow keys walk the matches, Enter takes the highlighted command instead of sending the half-typed line, and Escape closes the list. The message box is now the combobox that owns the list and reports the active option.
