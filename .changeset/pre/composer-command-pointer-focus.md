---
"@getdomovoi/ui": patch
---

Return focus to the message box after a composer command is chosen with the pointer, so typing the command's argument continues in the message instead of going nowhere.
