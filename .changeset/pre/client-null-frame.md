---
"@getdomovoi/ui": patch
---

A `null` frame from the daemon no longer throws inside the desktop and web client's message handler. Like a number or an array, it is reported as a message the client could not classify.
