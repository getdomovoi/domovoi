---
---

Test-only: coverage runs from each package's test script, so a focused vitest run reports only its tests; the CLI, the mobile app and the credential store gain coverage floors; the daemon's spawned-fixture wait ends when the fixture exits and prints what it wrote. No package changes.
