---
"@getdomovoi/ui": patch
---

Hold the machine sheet's opener above the pin and unpin swaps, so closing the sheet after a pin cycle returns focus to the rail control that opened it instead of to the document body.
