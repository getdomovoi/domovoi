---
"@getdomovoi/cli": patch
---

`domovoi doctor` reports the protocol probe with the shared compatibility rule: compatible when
major and minor match, otherwise which side is behind and what to update.
