---
"@getdomovoi/mobile": patch
---

When the daemon refuses a decision with a message that ends "The approval is still waiting.", the phone quotes that message and no longer adds its own "The gate is still waiting." after it. Other refusals keep the phone's line.
