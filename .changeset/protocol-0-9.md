---
"@getdomovoi/protocol": minor
"@getdomovoi/daemon": minor
---

The wire protocol is 0.9.0. Every wire change is a minor bump (ruled 2026-09-23), and 0.9 is the
minor that the read-only terminal methods, per-model image input, the pairing address on issued
codes, the archived branch and unmerged count, and session search ride on. A client on 0.8 is
refused at `system.hello` with `protocol-mismatch` (`compatibility: "machine-ahead"`) instead of
failing to parse a result that grew a field. Update the daemon and every client together.
