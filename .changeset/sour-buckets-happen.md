---

---

No release: add an experimental relay codec and shared deterministic fixtures to
the daemon and phone test runners. The codec has no production export or caller;
its crypto libraries are development dependencies. Production protocol schemas,
runtime dependencies, and published files are unchanged.
