---
"@getdomovoi/ui": patch
---

The Checkpoints tab can take a checkpoint, with an optional label, without going through the command palette. It is disabled while a turn runs and absent while watching.
