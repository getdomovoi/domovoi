---
"@getdomovoi/desktop": patch
"@getdomovoi/ui": patch
---

Settings offers Update the service when the login service runs an older Domovoi than this app. The desktop updates the service in place with the daemon's own update, refuses while a turn runs or a gate waits, and shows the daemon's words when the update does not finish.
