---
"@getdomovoi/mobile": patch
"@getdomovoi/ui": patch
"@getdomovoi/protocol": patch
---

Surfaces say what exists: the phone's empty states print the pair command the machine really runs and no installer or `domovoi new`; the pairing credential is described as it is scoped; the pairing card says pause and resume rather than start and stop; the fleet's UPDATE badge, which had no update path, is gone.
