---
"@getdomovoi/ui": minor
---

History rows say what happened in one line, filters are named as the design does with Turns first, checkpoints are reachable as a view of history, and a checkpoint row asks for fork and restore separately. Session-start rows carry the turn meta and drop fork.
