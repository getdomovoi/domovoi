---
"@getdomovoi/mobile": minor
---

The phone draws text from a generated type ramp and enforces its own type floor in every screen and the tab bar, so no phone text falls under the size the design system sets for it.
