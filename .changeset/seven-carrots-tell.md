---

---

Build and release tooling only. Add explicit desktop signing policy, protected native build
configuration and vendor credential setup. No runtime package or wire change is released by
this changeset; actual signatures still require the maintainer's native build and credentials.
