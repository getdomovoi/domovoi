import { version } from "../package.json"

// Generated into the bundle from release metadata. All workspace packages ship
// in lockstep; browsers and phones need neither filesystem access nor Node APIs.
// This identifies the executable release, not wire compatibility (protocolVersion).
export const buildVersion: string = version
